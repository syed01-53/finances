# Local development
VITE_API_URL=http://localhost:8000/api

# Production (Vercel) — REQUIRED before deploy, then redeploy:
# VITE_API_URL=https://finances-production.up.railway.app/api
# (use your exact Railway domain — must end with .railway.app and include /api)
