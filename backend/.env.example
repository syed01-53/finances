# Local development
DATABASE_URL=postgresql://postgres:admin123@localhost:5432/aw_client_portal
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174
LOG_LEVEL=INFO

# Production (Railway) — set these in the Railway dashboard:
# DATABASE_URL=<provided by Railway PostgreSQL plugin>
# CORS_ORIGINS=https://your-app.vercel.app
# CORS_ALLOW_VERCEL_PREVIEWS=true
# LOG_LEVEL=INFO

# Optional: path to WeasyPrint DLLs on Windows (MSYS2 Pango install)
# WEASYPRINT_DLL_DIRECTORIES=C:\msys64\mingw64\bin
